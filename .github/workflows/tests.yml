name: CI

on:
  # Trigger the workflow on push or pull request,
  # but only for the master branch
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  skip_duplicate:
    runs-on: ubuntu-latest
    outputs:
      should_skip: ${{ steps.skip_check.outputs.should_skip }}
    steps:
      - id: skip_check
        uses: fkirc/skip-duplicate-actions@master
        with:
          github_token: ${{ github.token }}
  test-pip:
      needs: skip_duplicate
      if: ${{ needs.skip_duplicate.outputs.should_skip == 'false' }}
      strategy:
        matrix:
          # Must stay within the requires-python floor declared in pyproject.toml.
          python-version: [ "3.11", "3.12", "3.13" ]
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@v4
        # Deliberately unpinned: uv.lock is written in a revision that older uv
        # releases cannot parse, and a pin here silently outlives the lock format.
        - name: Install uv
          uses: astral-sh/setup-uv@v9
          with:
            enable-cache: true
        - name: Set up Python ${{ matrix.python-version }}
          run: uv python install ${{ matrix.python-version }}
        - name: Install dependencies
          run: uv sync --dev
        - name: Run flake8
          run: uv run make flake8
        - name: Run mypy
          run: uv run make mypy
        - name: Check license
          run: |
            git clean -fd
            uv run make check-license
        - name: Check format
          run: |
            git clean -d -f -f
            uv run make check-format
        - name: Check notebook outputs carry no local paths
          run: uv run make check-notebooks
        - name: Test with pytest
          run: uv run make coverage
        - name: Run notebooks
          run: uv run make run-notebooks
        - name: Upload coverage
          uses: codecov/codecov-action@v1
          with:
            token: ${{ secrets.CODECOV_TOKEN }} # not required for public repos
          env:
            GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }} # This token is provided by Actions, you do not need to create your own token